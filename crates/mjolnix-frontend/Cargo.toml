[package]
name = "mjolnix-frontend"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1"
crossterm = "0.29.0"
mjolnix-shared = { path = "../mjolnix-shared" }
ratatui = "0.30.0"
tokio = { version = "1", features = [
  "io-util",
  "macros",
  "process",
  "rt-multi-thread",
] }
